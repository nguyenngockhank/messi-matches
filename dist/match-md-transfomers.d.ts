import { EnrichedMatch } from "./types";
export declare function matchResultIcon(match: EnrichedMatch): string;
export declare function roundTitle(match: any): string;
export declare function matchTitle(match: EnrichedMatch): string;
export declare function matchToMdContent(match: EnrichedMatch): string;
